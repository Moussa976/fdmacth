<form method="post" action="{{ path('app_matche_delete', {'id': matche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matche.id) }}">
    <button class="btn">Delete</button>
</form>
